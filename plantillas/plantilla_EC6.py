def pregunta_1(n: int) -> str:
    """
    Parámetros:
    n (int): Tamaño del cuadro

    Retorna:
    str: Cuadro formado con números consecutivos
    """
    return None


def pregunta_2(numero: int) -> bool:
    """
    Parámetros:
    numero (int): Número entero positivo.

    Retorna:
    bool: True si el número es especial, False en caso contrario.
    """
    return None


def pregunta_3(nombre: str) -> str:
    """
    Parámetros:
    nombre (str): Nombre completo de la persona.

    Retorna:
    str: Iniciales en mayúsculas separadas por puntos,
         o 'ERROR' si el nombre tiene menos de dos palabras.
    """
    return None


def pregunta_4(lista: list) -> list:
    """
    Parametros:
    lista (list) : una lista de numeros
    Retorna:
    list : es la lista filtrada
    """
    return None
