def pregunta_1(n: int) -> str:
    """
    Parametros:
        n (int): numero entero.
    Retorna:
        str: cadena de caracteres que tiene la escalera
    """
    return None


def pregunta_2(r1: float, r2: float, r3: float) -> float:
    """
    Parametros:
        r1(float) : radio de la esfera 1
        r2(float) : radio de la esfera 2
        r3(float) : radio de la esfera 3
    Retorna:
        float : el mayor volumen
    """
    return None


def pregunta_3(cadena: str) -> str:
    """
    Parametros:
        cadena (str) : cadena de texto
    Retorna:
        str : la cadena modificada
    """
    return None


def pregunta_4(lista: list) -> list:
    """
    Parametros:
        lista (list) : una lista de numeros
    Retorna:
        list : es la lista normalizada
    """
    return None
